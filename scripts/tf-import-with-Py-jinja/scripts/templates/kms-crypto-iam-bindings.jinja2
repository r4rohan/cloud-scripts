resource "google_kms_crypto_key_iam_binding" "crypto_key" {
  crypto_key_id = google_kms_crypto_key.key.id
  role          = "{{ iambinding_roles }}"

  members = [
    "{{ iambinding_member }}",
  ]
}
